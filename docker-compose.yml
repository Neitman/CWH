services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

  # backend:
  #   build: ./backend
  #   ports:
  #     - "4000:4000"
  #   environment:
  #     - REDIS_URL=redis://redis:6379
  #   depends_on:
  #     - redis
  #   env_file:
  #     - ./backend/.env

  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "5173:5173"
  #   environment:
  #     - VITE_BACKEND_URL=http://backend:4000
  #   depends_on:
  #     - backend

volumes:
  redis-data: